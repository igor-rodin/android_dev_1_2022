<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="android.view.View"/>
        <variable
            name="userViewModel"
            type="ru.igor.rodin.retrofit.ui.RandomUserViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="@dimen/vert_margin_middle"
        tools:context=".ui.RandomUserFragment">

        <include
            android:id="@+id/user_card"
            layout="@layout/user_card"
            app:userViewModel="@{userViewModel}" />


        <ProgressBar
            android:id="@+id/progress_bar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="@dimen/vert_margin_middle"
            android:visibility="@{userViewModel.state.loading ? View.VISIBLE : View.GONE}"
            app:layout_constraintBottom_toTopOf="@id/btn_update"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_update"
            style="@style/Widget.Material3.Button.OutlinedButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="@dimen/hor_margin_big"
            android:layout_marginBottom="@dimen/hor_margin_big"
            android:text="@string/btn_update_user_caption"
            android:enabled="@{!userViewModel.state.loading}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
